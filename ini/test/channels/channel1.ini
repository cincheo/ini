
process main() {
	@init() {
		i = 1
		suffix = "_suffix"
		channel("test_channel"+suffix).produce("coucou "+i)
	}
	c:@consume(v) [from=channel("test_channel"+suffix)] {
		println("string value = "+v)
		stop(c)
	}
}
