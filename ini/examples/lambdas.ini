
import "ini/lib/collect.ini"

process main() {
	@init() {
		li = [1, 2, 3, 4, 5, 6]
		li.map(e => return e+1).foreach(e => print(e))
		li.select(e => {
			mod(e, 2) == 0 {
				return true
			}
			@end() {
				return false
			}
		}).foreach(e => print(e))
	}
}
