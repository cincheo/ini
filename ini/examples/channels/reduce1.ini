
import "ini/lib/chanop.ini"
import "ini/lib/collect.ini"

s = "this is a text"

c = channel()
s.foreach(letter => c.produce(letter))
result = channel().map(c, letter => [letter, 1]).reduceByKey((a, b) => a + b)

c.stop()

println(result)
